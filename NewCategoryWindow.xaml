<Window x:Class="MM.NewCategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MM"
        mc:Ignorable="d"
        Title="NewCategoryWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20" />
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="20" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="username" Grid.Column="1" Grid.Row="1" Width="300" Height="40" FontSize="27" Text="Nazwa kategorii" Opacity="0.5" IsReadOnly="True"/>
        <Button x:Name="NewCategoryButton" Grid.Column="1" Grid.Row="2" Height="60" Width="200" VerticalAlignment="Center" Content="Dodaj"/>
        <Button x:Name="ExitButton" Grid.Column="1" Grid.Row="3" Height="60" Width="200" VerticalAlignment="Center" Content="Wróć do profilu"/>

    </Grid>
</Window>
